from the test directory:

cd ..;python -m build;pip install dist/*.whl --force-reinstall;cd tests;python quick_test.py
cd ..;python -m build;pip install dist/*.whl --force-reinstall;cd tests;python idea.py


https://github.com/tinycthread/tinycthread


compiler flags:

  -std=c99
  -std=c11
  -std=c17

  -Wall
  -Werror
  -Wextra (I THINK THIS DIDNT WORK ON WINDOWS)
  -pedantic (what does this actually do?)

  -fsanitize=address

  -O2
  -O3


mostly complete:

  refactor python with (un)/pack cov/data/knowledge functions

  suborder refactor
  incorporate knowledge


todo:

  check if we are leaking memory
  set up valgrind test

  check struct member order for alignment

  score refactor
  group varibles refactor (dg score)

  incorporate graph representation
  implement dag -> cpdag
    consider a bunch of other graph untility function
    implement RICF
    implement D/MaO mehtods

  implement sp -- mostly done
  implement grasp - need graph representation
  implement a* search on permutations?
  implement bes
    consider adding branch and bound bes

  imeplement delve
    this introduces set representations
    need set -> graph functions
      not too bad for ugs and dags

  NOTE: lvBES and set -> mag will have many similar issues to solve